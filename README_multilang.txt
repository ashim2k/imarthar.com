Multi-language translator package

File 1: translator_multilang.html
- Based on your original UI
- Adds target language dropdown
- Uses LibreTranslate for non-Bangla outputs

Next step:
You still need to wire the corpus-result -> translateFromBangla() call in your exact-match logic.
This starter file preserves your original design and is safe to test locally.
